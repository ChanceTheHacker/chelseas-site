const config = require("./config/config")
const plugins = require("./config/plugins")

module.exports = {
  siteMetadata: {
    title: `Chelsea Peragine`,
    description: `Real Estate`,
    author: `Chance Peragine`,
  },
  plugins,
}
